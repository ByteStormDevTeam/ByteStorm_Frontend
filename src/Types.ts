export type BlogType = {
    title: string;
    date: string;
    content: string;
    imageRef?: string;
    _id: string;
};

export type LeaderType = {
    name: string;
    score: number;
    _id: string;
};
